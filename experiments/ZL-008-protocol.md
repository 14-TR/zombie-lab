# ZL-008 preregistration / frozen inter-worker contract

Base a87211fcac93c53c5614ffb6107bb886d96dca13. Before outcome execution. Question: does THIS model-based two-tick policy improve on greedy? Improvement not assumed. No general intelligence claim, nor isolating depth from model/scoring (a later one-tick ablation could do that).

## Fixed policy
Unchanged simulation.js baseline. Experimental planner.js classic global ZombiePlanner exports chooseHuman(state), step(state). step uses unchanged ZombieLab.resolveTick. Zombie move MUST use old-state nearest Manhattan choice N,E,S,W,stay; may obtain from ZombieLab.step(state).zombie where safe. Never let zombie react to candidate new human location in same tick.
At each nonterminal state enumerate every legal two-human-action sequence, N,E,S,W,stay order each depth. Predict zombie deterministically at each old simulated state; resolve true pre/post cardinal/shared/crossing capture. No future information oracle beyond known fixed model. Horizon exactly two transitions, replan after first executed move. Hypothetical search must ignore real clock cutoff (use copied tick0 limit10000) so policy time-independent. Stop branch on capture. Score leaf lexicographically: number of completed NONCAPTURING transitions (0,1,2), then endpoint Manhattan distance. Maximize; exact ties retain first sequence in traversal order. A terminal captured branch gets its actual endpoint distance; no lookahead after contact. Execute only first human action via real resolveTick and unchanged real tick/cap. Initial captured/limit states follow baseline normalization, consume no step. No memory/randomness/training.

## Dataset and runner
Engine owns planner.js scripts/build-planning.cjs scripts/test-planning.cjs experiments/ZL-008-lookahead.md. Export buildPlanning and classifyRun if helpful. CLI `node scripts/build-planning.cjs preview` creates ONLY planning.json and planning-data.js (globalThis.ZombiePlanningData safely escaped). Source metadata PREVIEW_COMMIT. Frozen JSON:
{schemaVersion:1,metadata:{commit:string},config:{width:10,height:7,safetyTickLimit:10000,horizon:2,policy:'two-tick-model-based',excluded:'same-cell starts'},summary:{total:4830,baseline:{capture:n,cycle:n,unresolved:n},treatment:{capture:n,cycle:n,unresolved:n},later:n,earlier:n,same:n,escape:n,unresolved:n},results:[{id:'z<zid>-h<hid>',zombieId,humanId,baseline:{outcome:'capture'|'cycle'|'unresolved',stopTick,cycleStart:null|n,period:null|n},treatment:{same fields},comparison:'later'|'earlier'|'same'|'escape'|'unresolved'}]}
Order zombie0..69 then human0..69 excluding same cell. Includes246 initial adjacent cases captured0. Baseline should all capture, assert frozen original per-row. Comparison uses capture time difference only when both captured; treatment cycle vs captured baseline='escape' (indefinite avoidance under fixed deterministic rules); unresolved separate. Summary categories partition all4830. Complete data not bulk trajectories. Detect ordered position cycle with first occurrence, repeated endpoint included, capture->cycle->cap. For compute efficiency permissible cache planner choice by positions: report timing/compute transparently, no unbounded per-tick VM overhead. Deterministic position-only policy permits cycle proof; independent oracle compares every nonterminal choice as well as outcomes.

## Viewer
Viewer owns planning.html planning-view.js scripts/test-planning-view.cjs ONLY. Classic scripts simulation.js planner.js planning-data.js planning-view.js in that order; no other explorer auto-mount. Same-start paired baseline vs planner, shared controls clamp terminal/cycle endpoint and label frozen. Cycle replay displays first repeat endpoint not pretended capture/infinite elapsed time. All4830 choices via zombie/human selectors; numeric/legend map color later/earlier/same/escape/unresolved. Default first escape if any else largest positive captured delta if any else first difference else originalz42h27, clearly state selection criterion. Show aggregate all categories and nontrivial246 initial terminals context, full selected histories, predicted vs real constraints. Outcome computed with actual baseline/planner.step and matched to row; mismatch visible disable. Link compare.html/sweep.html/report. Node22-only tests, cached browser optional, no install. Parent copies assets into preview, workflows and report path.

## Oracle
Independent worker reads this protocol and simulation.js, not planner/engine results. Own only /tmp/zl008-oracle.py and /tmp/zl008-oracle.json; schema results/summary same as above plus decisions rows for nonterminal starts {id,human:{x,y}}. Independently implement simulation and exact scoring/traversal, enumerate all4830. Compare baseline to saved original. No repo edits or imports from new engine.

All source authors strict TDD. No commits/push by workers. Parent owns README/workflows/evidence packaging/review/publication. No simulation.js or previous evidence changes. <=15min each worker; disclose blockers rather than guesses.
