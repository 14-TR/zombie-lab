# ZL-009 frozen protocol — before outcome execution

Base0b6756dcc20255ce627a37dbc0be15f71980bc1f. Question: does model-based human planning still enable avoidance against two independent greedy pursuers? No assumption of benefit. No GRU or learning. Negative outcomes do not prove unavoidable capture.

## Fixed scope
Board10x7, N/E/S/W/stay ordered legal moves, simultaneous old-state choices, one move per agent. Z1 fixed initial(2,4), ID42. Enumerate Z2 IDs0..69 then human IDs0..69 excluding H42 and H=Z2; allow Z2=42 (co-located zombies). Exactly4761 conditions per policy,14283 runs. This is a fixed-Z1 slice, NOT all possible three-agent starts. Zombie identities retained ordered throughout; both chase nearest Manhattan to old human independently; no blocking, coordination, exclusion or collision handling between zombies. Either zombie captures human at shared/cardinal contact before or after move; retain pairwise crossing compatibility. Initial contact stops tick0. Capture beats cap. Same-cell human/zombie initial starts excluded, adjacent included. Cap10000 only experiment; cycle key includes human and BOTH ordered zombies; first repeated endpoint included; capture then cycle then unresolved. No memory/randomness.

## Policies
Greedy: human legal action maximizing MIN Manhattan distance from candidate human to the TWO OLD zombie positions; exact ties first N/E/S/W/stay.
One-step and two-step: shared exact known zombie model and scoring. Enumerate all legal human action sequences up to depth1 or2, depth-first N/E/S/W/stay, predict each zombie from its OLD simulated state and resolve simultaneous capture. Stop captured branches immediately. Score lexicographically completed NONCAPTURING transitions, then MIN endpoint Manhattan distance to either zombie. Maximize, first exact tie retained. Execute only first move and replan. Hypothetical copied clock0 cap10000 (no dependence on actual remaining cap), real clock enforced. Additional lookahead alone differs between depth1/2. Greedy vs model policies changes both scoring and prediction; disclose.

## Engine ownership/API
Worker1 ONLY two-zombies.js scripts/build-two-zombies.cjs scripts/test-two-zombies.cjs experiments/ZL-009-two-zombies.md. Preserve all old files. Classic global ZombiePair exports initialState(humanId=27,zombie2Id=42) -> {width,height,tick:0,tickLimit:10000,human:{x,y},zombies:[{x,y},{x,y}],status:'running',reason:''}; chooseHuman(state,policy) -> {x,y}; step(state,policy) -> state with policy one of 'greedy','depth1','depth2'. Actual terminal statuses running/caught/limit. Full coordinates stable, no shared mutations. Optional resolveTick exported for tests.
CLI `node scripts/build-two-zombies.cjs preview` writes ONLY two-zombies.json and two-zombies-data.js; globalThis.ZombiePairData safely escaped. buildTwoZombies({commit?}) export plus classifyRun if helpful. Complete scalar frozen schema:
{schemaVersion:1,metadata:{commit:string},config:{width:10,height:7,zombie1Id:42,safetyTickLimit:10000,policies:['greedy','depth1','depth2'],scope:'fixed-zombie1-slice'},summary:{total:4761,initialCapture:number,policies:{greedy:{capture,cycle,unresolved},depth1:{capture,cycle,unresolved},depth2:{capture,cycle,unresolved}}},results:[{id:'z2-<zid>-h<hid>',humanId,zombie2Id,outcomes:{greedy:{outcome:'capture'|'cycle'|'unresolved',stopTick,cycleStart:null|number,period:null|number},depth1:{same},depth2:{same}}}]}
Rows exact Z2 then human order. No bulk trajectories. Cache deterministic choices if needed but verify against uncached. Colocated zombies must reduce exactly to single-zombie greedy and depth2 for all69 human starts using old simulation.js/planner.js controls; can test one-zombie model depth1 independently. Report paired contingency matrices for greedy/depth1/depth2, both-captured time earlier/later/ties separately; never subtract cycle timestamp from capture tick. Measured time/RSS/bytes. Bound wall watchdog, no fabricated outputs.

## Viewer ownership
Worker2 ONLY two-zombies.html two-zombies-view.js scripts/test-two-zombies-view.cjs. Classic scripts two-zombies.js two-zombies-data.js two-zombies-view.js. Three same-start panels greedy/depth1/depth2 with common timeline, frozen capture/first-repeat endpoints labeled, full histories incl both zombies. Z2/human selectors exclude H42/HZ2, distinguish co-located zombies visually/textually. Outcome map for selected policy; aggregate per-policy numbers and initial-contact context. Default first row with differing outcomes among policies else differing finite capture times else original h27,z2=42; label selection and fixed-Z1 slice. Recompute selected actual trajectories and compare all outcome fields; visible mismatch clears/disables. Phone readable no new packages. Reports at experiments/ZL-009-two-zombies.md plus protocol, old planning.html navigation. Parent packages links. Tests Node22 builtins; cached optional browser distinct from runtime.

## Independent oracle ownership
Worker3 only /tmp/zl009-oracle.py /tmp/zl009-oracle.json. Read this protocol and old simulation/planner (not new engine/results). Independently implement both-zombie transition and all3 policies; enumerate same rows/schema/summary. Save decisions for every nonterminal enumerated start/policy {id,policy,human:{x,y}}. Test overlapping-zombie equivalence, swap/adjacency, opposite pursuers simultaneous choice, policy depth and ties, cycle key both zombies, cutoff. No repo edits. Parent compares every row/decision.

All workers strict TDD, no installs commits push; parent owns README/workflows/evidence/review/merge/live verification. <=15min each work block; disclose resource blocker. Prior scientific artifacts immutable.
